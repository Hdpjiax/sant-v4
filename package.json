{
  "name": "santander-demo",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "build": "node scripts/build.js",
    "dev": "node scripts/dev-server.js",
    "dev:local": "node scripts/dev-server.js",
    "dev:dist": "npx serve dist -l 3000",
    "preview": "npm run build && npm run dev:dist",
    "setup:pdf": "pip install -r requirements.txt",
    "serve:ios": "node scripts/serve-ipa.js"
  },
  "dependencies": {
    "@capacitor/core": "^8.5.0",
    "@capacitor/filesystem": "^8.1.2",
    "@capacitor/ios": "^8.5.0",
    "@capacitor/share": "^8.0.1"
  },
  "devDependencies": {
    "@capacitor/android": "^8.5.0",
    "@capacitor/assets": "^3.0.5",
    "@capacitor/cli": "^8.5.0"
  }
}
